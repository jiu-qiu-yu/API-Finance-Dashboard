## 1. 基础样式与卡片组件

- [x] 1.1 创建 `ui/styles.py` 样式常量模块，定义颜色、字体、间距等设计令牌
- [x] 1.2 创建 `ui/card_widget.py` 可复用卡片组件（QFrame + 圆角 + 阴影效果）
- [x] 1.3 创建 `ui/collapsible_panel.py` 可展开/收起面板组件（带平滑动画）

## 2. 全站中文本地化

- [x] 2.1 将 `main_window.py` 中所有英文文字替换为中文（窗口标题、按钮、标签、提示）
- [x] 2.2 将 `settings_panel.py` 中所有英文文字替换为中文（表单标签、按钮、对话框）
- [x] 2.3 将 `status_list.py` 中所有英文文字替换为中文（列头、状态标识）
- [x] 2.4 将 `inspection_worker.py` 中的进度文字替换为中文

## 3. 卡片式仪表盘首页

- [x] 3.1 重构 `main_window.py` 布局：顶部今日盈利卡片（占满宽度）
- [x] 3.2 实现今日盈利卡片：大号字体金额、公式提示、开始巡检按钮、上次巡检时间
- [x] 3.3 实现中间区域：左侧上游额度概览卡片 + 右侧告警摘要卡片（QGridLayout）
- [x] 3.4 实现告警摘要卡片：显示告急上游数量和名称列表，无告警时显示绿色 "所有上游额度正常"

## 4. 上游额度监控面板

- [x] 4.1 创建 `ui/quota_list_widget.py` 上游额度列表组件
- [x] 4.2 实现每个上游行：站点名称、当前余额、阈值、余额/阈值比例进度条
- [x] 4.3 实现进度条颜色逻辑：>100% 绿色、50%-100% 黄色、<50% 红色
- [x] 4.4 实现按状态排序：告急在前、正常在后

## 5. 阈值告警 UI

- [x] 5.1 修改 `status_list.py`：低于阈值的行自动标红背景 + 白色加粗文字
- [x] 5.2 在 `main_window.py` 的 `_on_finished` 中添加告警弹窗汇总逻辑
- [x] 5.3 在 `main_window.py` 的 `_on_finished` 中调用 `notifier.py` 发送系统通知（中文内容）

## 6. 盈利详情展开视图

- [x] 6.1 在今日盈利卡片中，使金额标签可点击（设置 cursor、hover 样式）
- [x] 6.2 实现展开面板：显示主站消耗、各上游消耗明细、上游总消耗
- [x] 6.3 实现跨货币展示：涉及汇率转换时标注原始货币金额
- [x] 6.4 点击金额切换展开/收起状态，使用 collapsible_panel 组件

## 7. 设置面板优化

- [x] 7.1 优化设置面板布局：使用更清晰的中文标签和分组
- [x] 7.2 确保阈值配置输入框有合理的默认值和校验提示

## 8. 集成测试

- [x] 8.1 验证全站中文文字正确显示
- [x] 8.2 验证卡片式布局在不同窗口尺寸下的响应表现
- [x] 8.3 验证阈值告警三层机制（标红 + 弹窗 + 系统通知）
- [x] 8.4 验证盈利详情展开/收起功能
